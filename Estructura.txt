SISTEMA
  -> Main
    -> Login
      ->Image
      ->Formulario

    (Si Ingresa credenciales correctas)
    -> Home
      -> NavBar
        -> LayoutSection
          -> Iconos
      -> UserBar
        -> Image
        -> Search
        -> Notification
        -> UserData
      ->Data
        (Se empieza en emitir pero depende del NavBar)
        ->Emitir
          ->DetailsDocument
          ->PDF
          ->DetailsProd
        ->Modificar
